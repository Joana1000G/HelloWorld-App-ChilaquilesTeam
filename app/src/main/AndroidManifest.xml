<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hellochilaquilesteam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloChilaquilesTeam">
        <activity android:name=".BookDetailActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateBookActivity" />
        <activity android:name=".FirebaseSingUpActivity" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".IntentActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SingUpActivity" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".LoingActivity">

        </activity>
    </application>

</manifest>